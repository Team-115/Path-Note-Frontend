import Map from "../../components/Map"

const Home = () => {
    return (
        <div>
            <Map
                width = "100%"
                height = "calc(100vh)"
            />
        </div>
    );
};

export default Home;